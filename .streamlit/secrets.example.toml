# Example only. Do NOT commit real secrets.
# In Streamlit Community Cloud, paste equivalent values into App Settings -> Secrets.

ANTHROPIC_API_KEY = "replace-me"
GITHUB_TOKEN = "replace-me"
OAUTH_ALLOWED_EMAILS = ["your.email@example.com"]

# Optional native Streamlit OIDC config example:
# [auth]
# redirect_uri = "https://your-app-name.streamlit.app/oauth2callback"
# cookie_secret = "replace-with-long-random-secret"
# [auth.google]
# client_id = "replace-me"
# client_secret = "replace-me"
# server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"
